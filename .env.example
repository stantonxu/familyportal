# Public env vars (exposed to the browser)
NEXT_PUBLIC_APP_NAME=Next on GCP
NEXT_PUBLIC_API_URL=https://api.example.com

# Server-side secrets (do not commit actual values)
API_TOKEN=
ANALYTICS_WRITE_KEY=
